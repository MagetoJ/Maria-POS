# render.yaml
# Updated to connect to an existing database.

services:
  - type: web
    name: maria-pos
    env: node
    plan: free
    
    # --- Build and Start Commands ---
    buildCommand: npm install && npm run build
    startCommand: node server/dist/index.js
    
    # --- Health Check ---
    healthCheckPath: /health

    # --- Environment Variables ---
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
     
        value: postgresql://mariahavens_user:BvEnYG8hb7baKudACcyxLuGkNgpxqloT@dpg-d3l72s3uibrs73cf7l60-a/mariahavens